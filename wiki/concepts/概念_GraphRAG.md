---
type: concept
tags:
- RAG/retrieval
summary: GraphRAG 是以图结构组织知识的检索增强生成方法，通过将文本知识构建成实体-关系图谱或层次化树结构，实现细粒度知识推理和复杂多跳查询，突破传统向量检索的局限。
sources:
- wiki/sources/RAG系统设计_语义搜索与KG驱动选型.md
- wiki/sources/优图RAG技术详解.md
- wiki/sources/跨模态知识联邦与统一语义推理RAG.md
- wiki/sources/再见RAG！AI知识库还得是SAG，又快又准～.md
updated: "2026-09-15"
---

# 概念_GraphRAG


## 定义

GraphRAG 是以图结构组织知识的检索增强生成方法，通过将文本知识构建成实体-关系图谱或层次化树结构，实现细粒度知识推理和复杂多跳查询，突破传统向量检索的局限。

## 两大类框架

1. **图结构方案**（GraphRAG、LightRAG）：知识粒度细化，但构图和检索质量/效率偏低，难以生产环境使用
2. **树结构方案**（RAPTOR、E2GraphRAG）：层次化迭代总结文本切片，但高度依赖大模型且无法挖掘细粒度知识关系

## 评测维度（优图 GraphRAG-Bench）

- 构图成本：离线构建图谱的时间和token消耗（RAPTOR token消耗最优）
- 检索效率：每次查询平均时长（RAPTOR向量检索最快）
- 回复准确率：GFM-RAG/GraphRAG/HippoRAG/RAPTOR领先
- 推理能力：HippoRAG和RAPTOR最佳

## 优图知识树结构

- 融合图（细粒度推理）和树（层次汇总）优点
- 四级知识粒度：属性/知识图(三元组)/关键词/社区
- 节点类型：实体和关系（连接语义单元）、属性（实体特征）、社区（核心信息总结）

## 离线全局构图 vs 查询时动态超边 (SAG)

传统 GraphRAG 与基于神经科学海马体索引理论的 HippoRAG 2 均依赖**离线静态预建全局知识图谱**：
- **痛点**：预提取全库实体三元组的 Token 与算力成本极其高昂，且面对频繁增删文档时，局部关系的重新计算与全局图谱维护复杂度高；
- **动态超边演进（SAG）**：[[concepts/概念_SAG|SAG 架构]]打破全局静态图谱范式，在入库时仅抽取单篇自包含的事项卡片与关联实体存入关系型 SQL 数据库，将图的组装时机推迟至**查询时（Query-Time）**，利用 SQL 的 `JOIN` 语句动态激活具有共享实体的局部超边网；在 MuSiQue（4跳地狱难度）基准上，SAG Recall@5 达 80.04%，相较 HippoRAG 2（65.13%）提升近 15 个百分点，且极大降低了增量维护成本。

## 关联

- 相关概念：[[概念_RAPTOR索引]]、[[概念_知识图谱RAG]]、[[概念_S2Dual社区检测]]、[[概念_知识树结构]]、[[概念_Agentic_RAG]]、[[概念_Lazy_Graph_RAG]]、[[概念_统一知识图谱]]、[[概念_SAG]]
- 来源：[[优图RAG技术详解]]、[[RAG系统设计_语义搜索与KG驱动选型]]、[[跨模态知识联邦与统一语义推理RAG]]、[[再见RAG！AI知识库还得是SAG，又快又准～]]