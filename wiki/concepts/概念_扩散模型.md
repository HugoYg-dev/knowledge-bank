---
sources:
- wiki/sources/DiT_扩散模型与Transformer.md
- wiki/sources/2026-07-27_The-anatomy-of-diffusion-LLMs_19fa57.md
summary: 扩散模型（Diffusion Model）通过前向加噪与反向去噪过程生成数据；涵盖图像生成领域的 DDPM/Latent Diffusion 以及自然语言生成领域的扩散大语言模型（dLLMs）非自回归并行去噪架构。
tags:
- CV/arch
type: concept
updated: '2026-09-21'
---
# 概念：扩散模型（Diffusion Model）

## 定义

高斯扩散模型假设一个前向加噪过程（Forward Noising Process），逐渐将噪声应用于真实数据；扩散模型学习一个反向过程：给定当前 step 的图片 x_t，通过网络预测上一 step 的图片 x_{t-1}。

## 要点

- **Reparameterization trick**：把随机变量改写为噪声变量的确定性函数，使得可通过梯度下降优化；前向过程可化简为一步直接采样 x_t
- **优化目标**：原始目标函数复杂，通过 variational lower bound 推导简化；训练时用预测噪声与 GT 噪声做对比
- **DDPM**（Denoising Diffusion Probabilistic Models）是开创性工作，首次引入基于 U-Net 骨干的扩散模型（U-Net 继承自 PixelCNN++，穿插空间 Self-Attention 块）
- 训练好后从噪声采样，通过 reparameterization 一步步采样出新图片

## 关联

- [[DiT_扩散模型与Transformer]]（来源）
- [[概念_Latent_Diffusion]]

---

## 跨领域延伸：扩散大语言模型（dLLMs，Diffusion LLMs）

随着大语言模型自回归生成面临严重的内存带宽墙（Memory Bandwidth Bound，每生成一个 token 必须读取全量权重与 KV Cache），业界开始将扩散模型去噪思想引入离散自然语言生成领域：

### 核心机制与计算特性转变
1. **全掩码到全局并行去噪**：dLLMs 从全掩码（Full-mask）序列出发，利用双向注意力机制在多个 step 中并行迭代预测并消除多个 token 的掩码，而非自左向右逐字生成。
2. **从访存受限转向算力受限**：由于每个去噪 step 能够同时处理并输出多个 token，单次前向传播能充分填满现代 GPU 的 Tensor Core 算力，显著提升 GPU 计算利用率（算存比提升数倍）。
3. **KV Cache 依赖消除**：无需维护庞大且易膨胀的自回归 KV Cache，显存开销固定且与长程上下文解码状态解耦。
